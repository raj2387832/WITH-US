[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm --filter @workspace/bg-remover run build"]

[start]
cmd = "npx serve artifacts/bg-remover/dist/public -s -l 3000"
